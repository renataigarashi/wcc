// Aqui está uma função com dois parâmetros `param1`e `param2`:

// function funcaoComParametros(param1, param2) {
//   console.log(param1, param2);
// }

// Então, podemos chamar `funcaoComParametros` assim: `funcaoComParametros("Olá", "Mundo");`. Passamos dois argumentos de string, `Olá` e `Mundo`. 

// Dentro da função, `param1`será igual à string `Olá` e `param2`será igual string `Mundo`. Observe que você poderia chamar `funcaoComParametros` novamente com argumentos diferentes e os parâmetros assumiriam o valor dos novos argumentos.

// 1. Crie uma função chamada `calculaSoma` que aceite dois argumentos e imprima o resultado da soma entre eles.
// 2. Chame a função com dois números como argumentos

function calculaSoma(param1, param2) {
    console.log(param1 + param2)
}

calculaSoma(2, 3);